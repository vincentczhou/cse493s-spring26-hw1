general:
  name: ${hydra:job.config_name}
  device: cpu

hydra:
  run:
    dir: outputs/inference/${general.name}/${now:%Y-%m-%d_%H-%M-%S}

checkpoint_dir: outputs/experiment/1.3_div_p97_2_493/2026-04-25_18-47-35/checkpoints
input_file: data/1.3_div_p97_test_noanswer.txt
